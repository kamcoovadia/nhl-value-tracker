{
  "name": "nhl-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "refresh": "node -e \"require('./src/dataFetcher').fetchAndCacheAllData().then(d => { console.log('Done:', d.players.length, 'players'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.14.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.0",
    "express": "^5.2.1",
    "node-cron": "^4.2.1",
    "node-fetch": "^3.3.2"
  }
}
