services:
  - type: web
    name: nhl-value-tracker-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: REFRESH_SECRET
        generateValue: true
    autoDeploy: true
    healthCheckPath: /api/health
